<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_helpdesk_ticket_attachment_line_list" model="ir.ui.view">
        <field name="name">helpdesk.ticket.attachment.line.list</field>
        <field name="model">helpdesk.ticket.attachment.line</field>
        <field name="arch" type="xml">
            <list create="0" delete="0">
                <field name="attachment_id" string="Fichero"/>
            </list>
        </field>
    </record>

</odoo>