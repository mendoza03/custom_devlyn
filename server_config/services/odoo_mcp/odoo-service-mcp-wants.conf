# Installed as /etc/systemd/system/odoo.service.d/10-odoo-mcp.conf.
# Include the MCP server whenever operators start Odoo after maintenance.
# odoo-mcp.service orders itself After=odoo.service, so it starts after Odoo.
[Unit]
Wants=odoo-mcp.service
