<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="repair_order_view_form_real_estate" model="ir.ui.view">
        <field name="name">repair.order.form</field>
        <field name="model">repair.order</field>
        <field name="arch" type="xml">
            <form string="Repair Order">
                <field name="unreserve_visible" invisible="1"/>
                <field name="reserve_visible" invisible="1"/>
                <header>
                    <button name="action_validate" invisible="state != 'draft'" type="object" string="Confirm Repair"
                            class="oe_highlight" data-hotkey="v"/>
                    <button name="action_repair_start" invisible="state != 'confirmed'" type="object"
                            string="Start Repair" class="oe_highlight" data-hotkey="q"/>
                    <button name="action_repair_end" invisible="state != 'under_repair'" type="object"
                            string="End Repair" class="oe_highlight" data-hotkey="x"/>
                    <button name="action_assign" invisible="state in ('draft', 'done', 'cancel') or not reserve_visible"
                            string="Check availability" type="object"/>
                    <button name="action_unreserve" type="object" string="Unreserve" invisible="not unreserve_visible"
                            data-hotkey="w"/>
                    <button name="action_create_sale_order" type="object" string="Create Quotation"
                            invisible="state == 'cancel' or sale_order_id"/>
                    <button name="action_repair_cancel" string="Cancel Repair" type="object"
                            invisible="state in ('done', 'cancel')" data-hotkey="l"/>
                    <button name="action_repair_cancel_draft" invisible="state != 'cancel'" string="Set to Draft"
                            type="object" data-hotkey="z"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,under_repair,done"/>
                </header>
                <sheet string="Repairs order">
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_sale_order" type="object" string="Sale Order" icon="fa-dollar"
                                class="oe_stat_button" invisible="not sale_order_id">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_value">
                                    <field name="sale_order_id" widget="statinfo" nolabel="1" class="mr4"
                                           invisible="1"/>
                                </span>
                                <span class="o_stat_text">Sale Order</span>
                            </div>
                        </button>
                        <button name="458" type="action" string="Product Moves" class="oe_stat_button"
                                icon="fa-exchange" invisible="state not in ['done', 'cancel']"/>
                    </div>
                    <div class="oe_title">
                        <label class="o_form_label" for="name"/>
                        <h1 class="d-flex">
                            <field name="priority" widget="priority" class="me-3"/>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="picking_product_ids" invisible="1"/>
                            <field name="picking_product_id" invisible="1"/>
                            <field name="tracking" invisible="1" readonly="True"/>
                            <field name="company_id" invisible="1"/>
                            <field name="sale_order_id" invisible="1"/>
                            <field name="sale_order_line_id" invisible="1"/>
                            <field name="repair_request" invisible="not sale_order_line_id"/>
                            <field name="partner_id" widget="res_partner_many2one"
                                   context="{'res_partner_search_mode': 'customer', 'show_vat': True}"
                                   readonly="sale_order_id"/>
                            <field name="project_id"/>
                            <field name="product_id" readonly="state in ['cancel', 'done']"
                                   domain="[('is_property', '=', True)]"/>
                            <field name="lot_id"
                                   context="{'default_product_id': product_id, 'default_company_id': company_id}"
                                   options="{'no_create': True, 'no_create_edit': True}"
                                   invisible="tracking not in ['serial', 'lot']"
                                   readonly="state == 'done' or tracking not in ['serial', 'lot']"
                                   required="tracking in ['serial', 'lot']"/>
                            <field name="product_uom_category_id" invisible="1"/>
                            <label for="product_qty" invisible="1"/>
                            <div class="o_row" invisible="1">
                                <field name="product_qty" readonly="tracking == 'serial' or state in ('done', 'cancel')"
                                       invisible="1"/>
                            </div>
                            <field name="picking_id" options="{'no_create': True}"
                            />
                            <field name="under_warranty" readonly="state in ['cancel', 'done']"/>
                        </group>
                        <group>
                            <field name="schedule_date" readonly="state in ['done', 'cancel']"/>
                            <field name="user_id" domain="[('share', '=', False)]"
                            />
                            <field name="company_id" options="{'no_create': True}"/>
                            <field name="tag_ids" widget="many2many_tags"
                                   options="{'color_field': 'color', 'no_create_edit': True}"/>
                            <field name="parts_availability_state" invisible="True"/>
                            <field name="parts_availability" invisible="state not in ['confirmed', 'under_repair']"
                                   decoration-success="parts_availability_state == 'available'"
                                   decoration-warning="parts_availability_state == 'expected'"
                                   decoration-danger="parts_availability_state == 'late'"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Parts" name="parts">
                            <field name="move_ids" readonly="state == 'cancel' or state == 'done'"
                                   context="{'default_repair_id': id, 'default_product_uom_qty': 1, 'default_company_id': company_id, 'default_date': schedule_date, 'default_repair_line_type': 'add'}">
                                <list string="Operations" editable="bottom">
                                    <field name="company_id" column_invisible="True"
                                    />
                                    <field name="name" column_invisible="True"/>
                                    <field name="state" column_invisible="True"/>
                                    <field name="repair_line_type" required="1"/>
                                    <field name="picking_type_id" column_invisible="True"
                                    />
                                    <field name="location_id" column_invisible="True"
                                    />
                                    <field name="location_dest_id" column_invisible="True"
                                    />
                                    <field name="partner_id" column_invisible="True" readonly="state == 'done'"
                                    />
                                    <field name="scrapped" column_invisible="True"/>
                                    <field name="picking_code" column_invisible="True"/>
                                    <!-- <field name="product_type" column_invisible="True"/> -->
                                    <field name="show_details_visible" column_invisible="True"/>
                                    <field name="additional" column_invisible="True"/>
                                    <field name="move_lines_count" column_invisible="True"/>
                                    <field name="is_locked" column_invisible="True"/>
                                    <field name="product_uom_category_id" column_invisible="True"/>
                                    <field name="has_tracking" column_invisible="True"/>
                                    <field name="display_assign_serial" column_invisible="True"/>
                                    <field name="product_id" context="{'default_detailed_type': 'product'}" required="1"
                                           readonly="(state != 'draft' and not additional) or move_lines_count &gt; 0"
                                    />
                                    <field name="description_picking" string="Description" optional="hide"/>
                                    <field name="date" optional="hide"/>
                                    <field name="date_deadline" optional="hide"/>
                                    <field name="product_uom_qty" string="Demand" readonly="state in ('done', 'cancel')"
                                    />
                                    <field name="forecast_expected_date" column_invisible="True"/>
                                    <field name="forecast_availability" string="Forecasted"
                                           column_invisible="parent.state in ('draft', 'done')" widget="forecast_widget"
                                    />
                                    <field name="product_qty" readonly="1" column_invisible="True"/>
                                    <field name="quantity" string="Done" readonly="not product_id"/>
                                    <field name="picked" string="Used"/>
                                    <field name="lot_ids" widget="many2many_tags"
                                           invisible="not show_details_visible or has_tracking != 'serial'"
                                           optional="hide"
                                           context="{'default_company_id': company_id, 'default_product_id': product_id}"
                                           domain="[('product_id','=',product_id)]"
                                    />
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report"
                                            icon="fa-area-chart" column_invisible="parent.state != 'draft'"
                                            invisible="forecast_availability &lt; 0 and repair_line_type == 'add'"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report"
                                            icon="fa-area-chart text-danger" column_invisible="parent.state != 'draft'"
                                            invisible="forecast_availability &gt;= 0 or repair_line_type != 'add'"/>
                                    <button name="action_show_details" type="object" icon="fa-list" width="0.1"
                                            title="Details" invisible="not show_details_visible"
                                            options="{&quot;warn&quot;: true}"
                                            context="{'default_location_dest_id': location_dest_id}"/>
                                </list>
                            </field>
                            <div class="clearfix"/>
                        </page>
                        <page string="Repair Notes" name="repair_notes">
                            <field name="internal_notes" placeholder="Add internal notes."/>
                        </page>
                        <page string="Miscellaneous" name="page_miscellaneous">
                            <group>
                                <field name="picking_type_id" options="{'no_create': True}" readonly="state != 'draft'"
                                />
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="repair_order_view_tree_inherit" model="ir.ui.view">
        <field name="name">repair.order.list</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="before">
                <field name="project_id"/>
            </xpath>
            <xpath expr="//field[@name='sale_order_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <!-- <xpath expr="//field[@name='address_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath> -->
        </field>
    </record>

    <record id="action_maintenance_order_list" model="ir.actions.act_window">
        <field name="name">Maintenance</field>
        <field name="res_model">repair.order</field>
        <field name="view_mode">list,form</field>
        <field name="view_ids" eval="[(5, 0, 0),
                (0, 0, {'view_mode': 'list', 'view_id': False}),
                (0, 0, {'view_mode': 'form', 'view_id': ref('real_estate_bits.repair_order_view_form_real_estate')})]"/>
        <field name="domain">[('is_maintenance', '=', True)]</field>
        <field name="context">{'default_is_maintenance': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No repair order found. Let's create one!
            </p>
            <p>
                In a repair order, you can detail the components you remove,
                add or replace and record the time you spent on the different
                operations.
            </p>
        </field>
    </record>

</odoo>
