<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_product_cancel_wizard_form" model="ir.ui.view">
        <field name="name">product.cancel.wizard.form</field>
        <field name="model">product.cancel.wizard</field>
        <field name="arch" type="xml">
            <form string="Archive Product(s)">
                <group>
                    <field name="cancel_reason"/>
                    <field name="cancel_type_id"/>
                    <field name="upload_files"/>
                    <field name="attachment_ids" widget="many2many_binary" invisible="not upload_files"/>
                </group>

                <footer>
                    <button string="Confirm"
                            type="object"
                            name="action_confirm"
                            class="btn-primary"/>
                    <button string="Cancel"
                            special="cancel"
                            class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_product_cancel_wizard" model="ir.actions.act_window">
        <field name="name">Archive Product(s)</field>
        <field name="res_model">product.cancel.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <record id="view_contract_send_email_wizard_form" model="ir.ui.view">
        <field name="name">contract.send.email.wizard.form</field>
        <field name="model">contract.send.email.wizard</field>
        <field name="arch" type="xml">
            <form string="Send Contract Email">
                <sheet>
                    <group>
                        <field name="rev_id" readonly="1"/>
                        <field name="body_text" placeholder="Write the email body here..."/>
                        <field name="attachment_ids" widget="many2many_binary"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_send_contract_email"
                            string="Send"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
