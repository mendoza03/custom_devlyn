<odoo>
  <record id="view_missing_documents_email_wizard_form" model="ir.ui.view">
    <field name="name">missing.documents.email.wizard.form</field>
    <field name="model">missing.documents.email.wizard</field>
    <field name="arch" type="xml">
      <form string="Send missing documents email">
        <group>
          <field name="rev_id" invisible="1"/>
          <field name="extra_message" placeholder="Write an additional note to the customer..."/>
          <field name="attachment_ids" widget="many2many_binary"/>
        </group>

        <footer>
          <button string="Send" type="object" name="action_send" class="btn-primary"/>
          <button string="Cancel" special="cancel" class="btn-secondary"/>
        </footer>
      </form>
    </field>
  </record>
</odoo>
